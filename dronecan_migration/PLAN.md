# DroneCAN libcanard migration — execution plan & tracker

Living tracker for replacing PX4's libuavcan DroneCAN-v0 driver (`src/drivers/uavcan/`, ~124 KB) with a libcanard-based `src/drivers/dronecan/` (~53 KB target, **~64 KB flash reclaimed** on `ark_fmu-v6x`: 99.9% → ~96.6%).

- **Frozen briefing (required prior reading):** [`../DRONECAN_LIBCANARD_MIGRATION.md`](../DRONECAN_LIBCANARD_MIGRATION.md) — motivation, flash evidence, the three reference points, the architectural deltas, the open questions. Do not edit; it is the stable design input.
- **Flash evidence / prior findings:** project memory `project-flash-bloat-icf`.
- **Execution model — *tactical backbone*:** a human-driven, plan-mode-gated sequential build, with Claude Code workflows dropped in only for the fan-out / cross-check phases. Workflows never drive the safety-critical sequential build; they understand, extract, port-in-parallel, and review.

## Locked design decisions (§2.1 — do not re-litigate)

1. **Vendor the legacy libcanard** (`dronecan/libcanard`) + `dronecan_dsdlc` as submodules under the new driver. *Not* cyphal's modern (Cyphal-v1) libcanard.
2. **`dronecan` ⊥ `cyphal`, `dronecan` wins.** Kconfig `DRIVERS_CYPHAL depends on !DRIVERS_DRONECAN` + a belt-and-suspenders CMake `message(WARNING)`. This resolves the `canard*` symbol collision — the two libcanard generations never co-link.
3. **New module `dronecan`** (`src/drivers/dronecan/`, CLI `dronecan start`), mutually exclusive with the old `uavcan`; a board migration is one Kconfig flip (`DRIVERS_UAVCAN` off → `DRIVERS_DRONECAN` on).
4. **Params `UAVCAN_*` → `DC_*`** with an idempotent startup migration that imports legacy values (16-char name limit makes `DC_` roomier).
5. **Preserve DNA + SD-card firmware serving** (`BasicFileServer` + `FirmwareUpdateTrigger` + FW DB under `/fs/microsd/ufw`). In scope, not deferred.

## Phases → Claude Code primitive

| Phase | Work | Primitive / tool | Gate | Status |
|---|---|---|---|---|
| **P0** Scaffold | docs, memory, reusable tooling, lock the plan | interactive + memory | — | ✅ done |
| **P1** Understand | reference maps of AP_DroneCAN / cyphal / uavcan | workflow `dronecan-reference-map` → `REFERENCE_MAP.md` | — | ✅ done |
| **P2** Acceptance spec | 16 bridges + 3 actuators → field-map oracle | workflow `dronecan-acceptance-spec` → `ACCEPTANCE_SPEC.md` | — | ✅ done |
| **P3** Design + spike | architecture; resolve §8 (esp. Q1 codec layer); vendor + generate 2-3 codecs, measure `.text` | workflow N-angle judge panel → **plan-mode lock** | 🔒 design lock | ✅ locked |
| **P4** Vertical slice | GNSS + battery + ESC + DNA + SD-FW on `ark_fmu-v6x`, behind `DRIVERS_DRONECAN` | interactive backbone + `/dronecan-flash-delta` → PR | 🔒 slice proven | ⏳ P4.5 (P4.0–P4.4 ✅) |
| **P5** Remaining bridges | 12 sensors + aux controllers | workflow `dronecan-port-bridge` (worktree isolation) | — | ⬜ |
| **P6** Heavy subsystems | MAVLink param bridge, RemoteID, time sync, log fwd, param-migration fn | interactive backbone | — | ⬜ |
| **P7** Validate + cutover | size before/after, ESC latency, SITL/HIL; Kconfig/`rcS` switch | interactive + workflow `dronecan-review` | 🔒 cutover | ⬜ |

**Sign-off gates (yours):** after **P3** (design lock), after **P4** (slice proven on a hardware-sized build), before **P7** (cutover). Each gate = a plan-mode review you approve before the next phase starts.

## Reusable toolkit (committed under `.claude/`)

| Tool | Phase | What it does | Runnable |
|---|---|---|---|
| `workflows/dronecan-reference-map.js` | P1 | 8 parallel readers → consolidated `REFERENCE_MAP.md` | **now** |
| `workflows/dronecan-acceptance-spec.js` | P2 | per-bridge field-map extraction → `ACCEPTANCE_SPEC.md` | **now** |
| `workflows/dronecan-design.js` | P3 | 4 architects → 3-lens judge panel → synthesized `DESIGN.md` | **now** |
| `workflows/dronecan-port-bridge.js` | P5 | port N bridges in worktrees, verify each vs the spec | after P4 (needs the example bridge) |
| `workflows/dronecan-review.js` | P7 | adversarial review vs spec + flash-size gate | after P4 |
| `skills/dronecan-flash-delta/` | P4–P7 | build a board, report DroneCAN `.text`/`.rodata` delta (§9.1) | **now** |

Run a workflow by asking me, e.g. *"run the dronecan-reference-map workflow"*, or `/dronecan-reference-map` once saved. Skill: `/dronecan-flash-delta <board> [ref-before] [ref-after]`.

## Feature-parity checklist (P4/P5/P6 — the acceptance surface, §5)

### Sensor bridges (16)
- [x] **GNSS** — `gnss.Fix`/`Fix2`/`Auxiliary` (+`ardupilot.gnss.RelPosHeading`) → `sensor_gps`  *(P4.4 ✅; incl. RTCM/MBD uplink + MBD→`gps_dump`)*
- [ ] GNSS relative — `ardupilot.gnss.RelPosHeading` → `sensor_gnss_relative`
- [ ] Baro — `air_data.StaticPressure`/`StaticTemperature`/`RawAirData` → `sensor_baro`
- [ ] Mag — `ahrs.MagneticFieldStrength`/`...2` → `sensor_mag`
- [ ] Accel — `ahrs.RawIMU` → `sensor_accel`
- [ ] Gyro — `ahrs.RawIMU` → `sensor_gyro`
- [ ] Airspeed — `air_data.IndicatedAirspeed`/`TrueAirspeed`/`StaticTemperature` → `airspeed`
- [ ] Diff pressure — `air_data.RawAirData` → `differential_pressure`
- [ ] **Battery** — `power.BatteryInfo` (+`BatteryInfoAux`, `cuav…CBAT`) → `battery_status`/`battery_info`  *(P4 slice)*
- [ ] Flow — `com.hex.equipment.flow.Measurement` → `sensor_optical_flow`
- [ ] Rangefinder — `range_sensor.Measurement` → `distance_sensor`
- [ ] Hygrometer — `dronecan.sensors.hygrometer.Hygrometer` → `sensor_hygrometer`
- [ ] Fuel tank — `ice.FuelTankStatus` → `fuel_tank_status`
- [ ] ICE status — `ice.reciprocating.Status` → `internal_combustion_engine_status`
- [ ] Safety button — `ardupilot.indication.Button` → `button_event`
- [ ] Battery filtered mode (`DC_SUB_BAT==2` → internal battery lib)

### Actuators
- [ ] **ESC** — out `esc.RawCommand` ≤400 Hz; in `esc.Status`/`StatusExtended` → `esc_status`  *(P4 slice)*
- [ ] Servo — `actuator.ArrayCommand` @50 Hz
- [ ] Hardpoint — `hardpoint.Command`

### Services & node management
- [ ] **DNA-v0 centralized allocation server**  *(P4 slice)*
- [ ] **SD-card FW/file server** (`BasicFileServer` + `FirmwareUpdateTrigger` + FW DB `/fs/microsd/ufw`)  *(P4 slice, decision 5)*
- [ ] NodeInfoRetriever / NodeStatusMonitor → `dronecan_node_status`
- [ ] NodeInfoPublisher → `device_information`; `can_interface_status`
- [ ] GlobalTimeSync master/slave
- [ ] Log forwarding (`debug.LogMessage` → `px4_log`)
- [ ] MAVLink remote-param bridge (GetSet / ExecuteOpcode / RestartNode)  *(P6)*

### Aux controllers (`CONFIG_UAVCAN_*`-gated)
- [ ] ArmingStatus, Beep (`tune_control`→`BeepCommand`), RGB LED (`LightsCommand`), SafetyState, RemoteID (bidirectional Open Drone ID), GNSS RTCM/MovingBaseline + PPK `gps_dump`

### Params + boot
- [ ] `DC_ENABLE` / `DC_NODE_ID` / `DC_BITRATE` / `DC_SUB_*` / `DC_PUB_*` / `DC_EC_*` / `DC_SV_*` + the `UAVCAN_*`→`DC_*` migration fn
- [ ] `rcS`: `if param greater DC_ENABLE 0; then dronecan start`

## Open questions (resolve in P3, §8)

> P1 produced **draft recommendations** for Q1/Q3/Q5/Q7 in `REFERENCE_MAP.md` §8 — to confirm (and, for Q1, measure 2-3 types) at the P3 design-lock gate.

1. **Typed codec layer** — ArduPilot's header-only `Canard::Publisher/Subscriber` + `cxx_iface` trait, or a lighter PX4 shim? *Measure 2-3 types early* — **highest-leverage**; the whole point is to avoid libuavcan's per-type explosion.
2. Scope/phasing — minimal vertical slice first ✓ (baked into P4).
3. DNA-server source — port AP's `StorageManager`-backed server vs re-host the current `UavcanServers` on libcanard. Either must keep allocation **and** SD-card FW serving.
4. MAVLink param bridge & RemoteID — sequence after the core slice ✓ (P6).
5. Memory model — legacy internal-pool sizing vs today's `HeapBasedPoolAllocator` (soft/hard blocks); provide a `dronecan shrink`?
6. Param-migration details — exact name map (16-char limit), run timing/idempotency, legacy retirement, how `rcS` selects `dronecan` vs `uavcan` during transition.
7. Validation — ESC output latency (immediate-flush vs WQ), SITL/HIL DroneCAN, `size -A` before/after on constrained boards.

## Current status

> **▶ Resume point — next session starts at P4.5 (battery bridge).** P4.0–P4.4 are done on `dronecan-libcanard` (scaffold `376dc0c8df`, transport `f6621d27fd`, P4.2 record `a7c70410cd`, shared FDCAN driver `011a250098`, P4.3 node wiring `38269c30e9`, P4.4 GNSS bridge — see the dated P4.4 entry below). `DronecanNode` now owns the transport: real `DronecanHandle` + `DronecanRxRouter` + a constructor-injected media backend, lazy first-`Run()` bring-up, `transmit(); receive(); transmit();` under `_node_mutex`, `canardSetLocalNodeID(DC_NODE_ID)` after `isUnicast`, NodeStatus @1 Hz (first real publisher via `encodeBroadcast`), `dronecan status`, `migrateLegacyParams()` + rcS `DC_ENABLE` gate. Build links on `ark_fmu-v6x`; codec test stays green. **Media:** the on-target backend is the **battle-tested STM32H7 FDCAN driver extracted from libuavcan into the platform layer** (`platforms/nuttx/src/px4/stm/stm32h7/can`), rewritten against a new framework-neutral shim (`platforms/common/include/px4_platform_common/px4can.hpp`) with an hrt-backed clock (no dedicated timer) and the CAN-RX-IRQ `BusEvent` wired to the node's `ScheduleNow()` trampoline — **zero `src/drivers/uavcan` dependency** (so libuavcan can be deleted at cutover; the shared driver outlives it). The node selects it on H7 (`DRONECAN_FDCAN_DRIVER`); the NuttX SocketCAN/CDev backends are demoted to future stubs, `NullCanardInterface` is the last-resort fallback. On-bus operation is pending hardware validation (not doable in this environment). **P4.4 (GNSS bridge) is done** — `src/drivers/dronecan/bridges/` now holds `SensorBridgeBase` (channel-alloc), the platform-free `GnssDecode` (every ACCEPTANCE_SPEC §1 quirk as a named pure function), and the `Gnss` bridge (an `RxSubscriberBase` registering Fix/Fix2/Auxiliary/RelPosHeading [+MovingBaselineData] with `_rx_router`, decoding through the codec shim, mapping to `sensor_gps`, with the RTCM/MBD uplink + `gps_dump`). It is the `/dronecan-port-bridge` reference. **P4.5 = port the battery bridge** (ACCEPTANCE_SPEC §9): the four BatteryInfo/BatteryInfoAux/CBAT decode paths with the exact precedence, per-instance accumulators, K→C, CBAT current negation, the second `battery_info` topic — model it on the `Gnss` bridge (same `SensorBridgeBase` + `RxSubscriberBase` + named-pure-function-per-quirk + field-for-field test shape). Its ctor takes a `NodeInfoPublisher*` (nullptr-guarded until P4.6). Full P4.4 detail in the dated entry below; architecture in [`DESIGN.md`](DESIGN.md) §8.

**2026-05-30:** P0 scaffolding complete. **P1 done** — `REFERENCE_MAP.md` written (8 readers + synthesis, ~760K tok) and spot-checked (PX4-side citations accurate); §7 caught real correctness traps, §8 carries draft recommendations for Q1/Q3/Q5/Q7. **P2 done** — `ACCEPTANCE_SPEC.md` (982 lines, 18 bridges, 584 citations), spot-checked accurate; a clean per-field porting oracle. Spec self-flags 3 spots for extra human review when ported: Battery (4 mode-paths; some NAN rows originate in `src/lib/battery`), GNSS Fix2 covariance fallthrough (`gnss.cpp:217`, latent bug), ESC `failures` (derived from uORB inputs, not the Status msg). **Design LOCKED (plan approved 2026-05-30); P4 in progress** on branch `dronecan-libcanard`. **P4.0:** 4 DroneCAN submodules vendored + pinned to AP commits (libcanard `601ed35`, dronecan_dsdlc `de47a89`, DSDL `04e0e81`, pydronecan `08cda37`); **codegen validated standalone** in PX4's env (206 headers / 176 srcs, rc=0) — confirms Q1: `_ID`/`_SIGNATURE`/`_MAX_SIZE` `#define`s present, `cxx_iface` trait `#ifdef DRONECAN_CXX_WRAPPERS`-guarded (off by default), decode returns TRUE-on-failure. **Implementation notes / deviations:** codegen wired **configure-time** (cyphal idiom — compiled `.c` needs it; DESIGN.md's "build-time" note was for the header-only libuavcan case); Kconfig mutual-exclusion follows **§2.1** ("dronecan wins" → `depends on !DRIVERS_DRONECAN` on cyphal + uavcan), not DESIGN.md P4.0's inverted paraphrase.

**2026-05-30 — P4.0 scaffold ✅ (build green on `ark_fmu-v6x`).** Created `src/drivers/dronecan/`: `CMakeLists.txt` (4× `px4_add_git_submodule`, configure-time `dronecan_dsdlc` codegen with `message(FATAL_ERROR)` on rc≠0, `file(GLOB)` of generated `src/*.c`, canard flags `TAO=1/CANFD=0/DEADLINE=1/SEM=0`, `DRONECAN_CXX_WRAPPERS` left undefined, `remove_definitions(-Werror)`, FATAL belt-and-suspenders if `CONFIG_DRIVERS_UAVCAN`/`CONFIG_DRIVERS_CYPHAL` co-set); `Kconfig` (`menuconfig DRIVERS_DRONECAN`, `depends on PLATFORM_NUTTX`); §2.1 edits to `cyphal/Kconfig` + `uavcan/Kconfig` (`depends on !DRIVERS_DRONECAN`); minimal `module.yaml`; `dronecan.params.yaml` (`DC_ENABLE`/`DC_NODE_ID`/`DC_BITRATE`); minimal `DronecanNode` (`ScheduledWorkItem` on `wq:uavcan` + `dronecan {start|status|stop}` CLI). Board toggle: `boards/ark/fmu-v6x/default.px4board` → `CONFIG_DRIVERS_DRONECAN=y`, `DRIVERS_UAVCAN` removed (uncommitted; the toggle under test). **Result:** `make ark_fmu-v6x` exit 0; 178 module objects (canard.c + 176 generated + node); `libdrivers__dronecan.a` linked; `dronecan_main` + `canardInit` are `T` symbols in the ELF; **0 uavcan objects** (mutual exclusion confirmed); whole-module compile **clean** (the build's lone `warning:` is the pre-existing board `ARCH_CHIP_UNSET`). FLASH 92.46% (1,817,752 B) — empty-scaffold baseline; the meaningful number is the P4.2 3-type seam measurement.

**2026-05-30 — P4.1 transport + codec + dispatch ✅ (contract test passes; compiles on `ark_fmu-v6x`).** Added the platform-free transport stack to `src/drivers/dronecan/`: `CanardInterface` (v0 media base + `DronecanRxFrame`, re-typed to `CanardCANFrame`); `DroneCANCodec` (the two error boundaries — `decodeTransfer` is the single TRUE-on-failure negation; `encodePayload`/`encodeBroadcast` the single 0-length-failure check; `TypeDescriptor` POD as data); `DronecanRxRouter` + `RxSubscriberBase` (per-instance `[dtid % DC_RX_BUCKETS]` buckets, broadcast→all / service→first, `accept`+`dispatch` walk the same bucket so the accepted signature always matches the decoder); `DronecanHandle` (the sole canard boundary — `canardInit` over a per-instance 8 KB static pool, the two `user_reference`→`this` trampolines, `receive`/`transmit`/`broadcast`/`requestOrRespond`/`cleanupStale`/`poolStats`, constructor-injected media, pool-exhaustion exposed as an observable counter). **Deliberately platform-free** (canard + std only, no perf/hrt/log) so the whole stack is drivable off-target; the PX4 glue (perf, hrt, real CAN media, logging) lands in the node in P4.3. **Gate met:** `test/dronecan_codec_test.cpp` (one-command `test/run_codec_test.sh`) passes against the real `canard.c` + generated `NodeStatus` codec — decode inversion (stub + real wrong-length, too-short and too-long), encode→decode identity, and full fake-frame dispatch (encode→broadcast→TX-drain→`FakeCanardInterface` loopback→`canardHandleRxFrame` reassembly→trampolines→router→subscriber→decode→identity; transfer-id auto-increment verified). All transport sources also compile clean on `ark_fmu-v6x` (gc-stripped until the node wires them in P4.3); build stays at 1 pre-existing warning. **Deviations:** the gate runs as a standalone host program, not `px4_add_unit_gtest` — dronecan is NuttX-only so its CMakeLists isn't processed on the SITL test build, which would leave a host gtest dead; CI integration (SITL-buildable codec, or relaxing the Kconfig gate) is a follow-up. `flush()` (ESC-only TX filter; needs queue-walking past non-ESC frames) and the real `CanardNuttXCDev`/`CanardSocketCAN` backends are deferred to P4.7/P4.3 (not needed for the codec gate). **Next:** P4.2 — reference 3 type descriptors (`gnss.Fix2`, `power.BatteryInfo`, `esc.RawCommand`) and run `/dronecan-flash-delta ark_fmu-v6x` to confirm per-type cost is flat (generated `_encode`/`_decode` only, no template/vtable growth).

**2026-05-30 — P4.2 Q1 seam measurement ✅ GO (codec layer is flat).** Measured per-type vs fixed cost directly from the `ark_fmu-v6x` build objects (`arm-none-eabi-size`/`nm`) — isolating the seam more precisely than a gc-stripped full-binary delta. **Per-type cost is ONLY the generated marshalling:** `gnss.Fix2` 1342 B, `power.BatteryInfo` 788 B, `esc.RawCommand` 220 B of `.text`, each with 0 data / 0 bss (no per-type static tables). **Fixed transport cost is O(1)** regardless of type count: `DroneCANCodec` 92 + `DronecanRxRouter` 116 + `DronecanHandle` 440 = **648 B** `.text` total. `nm --defined-only -C` on the three transport objects shows ONLY type-erased symbols (`encodePayload`/`decodeTransfer`/`encodeBroadcast` over `void*`+fn-ptr+`TypeDescriptor`; router/handle over `TypeDescriptor`) — **zero** per-type symbols, no `Publisher<T>`/`Subscriber<T>`/vtable/`msg_buf[MAX_SIZE]` instantiation (the libuavcan per-type explosion that is the ~64 KB bloat source). **Verdict: GO** — each new bridge type adds only its irreducible generated `_encode`/`_decode`; the typed surface stays flat in message count, validating the Q1 thesis the whole reclaim rides on. The optional `decode<T>` template was dropped for the non-templated `decodeTransfer` + thin per-type wrappers (DESIGN §9.2 "already ergonomic enough"), so there is no template to fold. **Note:** the *integrated* `/dronecan-flash-delta` is uninformative while the transport is gc-stripped (unreferenced); the end-to-end ~64 KB reclaim is the **P4.9** gate, measured once the slice references the types. **Next:** P4.3 — `DronecanNode` lifecycle: wire the handle + real CAN media on `wq:uavcan`, lazy first-`Run()` init, `transmit/receive/transmit` under `_node_mutex`, `canardSetLocalNodeID(DC_NODE_ID)`, NodeStatus 1 Hz, CLI, `migrateLegacyParams()`, rcS gate.

**2026-05-30 — P4.3 node ↔ transport wiring ✅ (links on `ark_fmu-v6x`; codec test green).** `DronecanNode` now drives the real transport. **Media:** ported `CanardNuttXCDev` + `CanardSocketCAN` from cyphal, re-typed to the v0 `CanardCANFrame` — TX masks the EFF/RTR/ERR flags libcanard packs into `id` (`& CANARD_CAN_EXT_ID_MASK`), RX OR-s `CANARD_CAN_FRAME_EFF` back in (canard.c:412 rejects non-EFF frames), SocketCAN RX **memcpy-s into the inline `data[8]`** (fixes cyphal's dangling-pointer repoint of the modern `CanardFrame::payload`, REF:267), TX deadline taken from `frame.deadline_usec`. CMake selects the backend exactly as cyphal does (`CONFIG_NET_CAN` → SocketCAN, `elseif CONFIG_CAN_EXTID` → NuttXCDev). **Node:** `DronecanRxRouter` + `DronecanHandle` members, constructor-injected media; dropped the P4.0 `canardInit` link-proof in `print_info()` for real handle ownership; lazy first-`Run()` bring-up (media `init()` + `canardInit` over the `DC_POOL` pool + `setNodeID`), retried each tick; `Run()` locks `_node_mutex` → lazy init → param update → `sendNodeStatus()` → **`transmit(); receive(); transmit();`** → `cleanupStale` once/pass → unlock; `canardSetLocalNodeID(DC_NODE_ID)` after an `isUnicast` (1..125) check; **NodeStatus @1 Hz is the first real publisher through `encodeBroadcast`** (uptime/HEALTH_OK/OPERATIONAL); `dronecan status` surfaces node id + `peak_usage_blocks` + `poolExhaustedCount`; CLI `start|stop|status|migrate|shrink(no-op)`. The `busevent_signal_trampoline → ScheduleNow()` seam + dual-scheduling are in place. **Params:** added `DC_POOL` (runtime bound, default 8192 / 2048..16384) + `DC_MIGRATED` sentinel; bumped the handle static pool ceiling (`DC_POOL_MAX`) 8192→16384 so `DC_POOL` can grow without a recompile. **Migration:** `migrateLegacyParams()` (static) — idempotent, `DC_MIGRATED`-gated, non-destructive (`param_value_is_default` → user `DC_*` wins), `{UAVCAN_ENABLE/NODE_ID/BITRATE → DC_*}` table; runs from rcS `dronecan migrate` (probed by `DC_BITRATE>0` so it's a no-op on non-dronecan boards) **before** the gate, and again at `dronecan start`. **rcS:** `if param greater -s DC_ENABLE 0; then dronecan start` ahead of the uavcan/cyphal branches. **Build:** `make ark_fmu-v6x` links (1300/1300, `.px4`); FLASH **92.69 %** (1,822,312 B) vs the P4.0 empty-scaffold 92.46 % — the slice now references transport/codec/NodeStatus for real (the ~64 KB reclaim remains the P4.9 gate). `canardBroadcastObj` + `canardHandleRxFrame` are `T` in the ELF (transport live via the node, not a link-proof); 0 new warnings; `run_codec_test.sh` green; all 8 changed/new files astyle-clean. **Deviations / honest gaps:** (1) brief said `CONFIG_CAN` for the NuttXCDev gate — mirrored cyphal's actual `CONFIG_CAN_EXTID` instead. (2) **On-bus media** was initially stubbed (the cyphal-style NuttX SocketCAN/CDev backends need `CONFIG_NET_CAN`/`CONFIG_CAN_EXTID`, which `ark_fmu-v6x` does not set — H7 has only `stm32_fdcan_sock` and no in-tree H7 board runs NuttX SocketCAN). **Resolved in P4.3a (below):** the libuavcan FDCAN register driver was extracted to a shared, framework-neutral platform driver and is now the on-target backend; the NuttX backends are demoted to future stubs. (3) `busevent` registration is a no-op until a media backend can signal RX readiness; the 3 ms tick drives RX meanwhile (matches cyphal). (4) migration only imports while the legacy `UAVCAN_*` defs stay registered (DESIGN §7 one-release retention) — a safe no-op until that retention is wired. Still deferred: `flush()` ESC-only TX filter (P4.7); codec-test `px4_add_unit_gtest`/CI integration (open). **Next:** P4.4 GNSS bridge.

**2026-05-30 — P4.3a shared CAN driver extraction ✅ (links on `ark_fmu-v6x`; codec test green).** Replaced the stubbed on-bus media with the **battle-tested STM32H7 FDCAN register driver, extracted from libuavcan into the platform layer** and decoupled from libuavcan so both DroneCAN stacks can share it. **Decisions (user-directed):** driver lives in `platforms/nuttx/src/px4/stm/stm32h7/can` (the obvious shared/arch home); the legacy `uavcan` module is left **untouched** (it keeps its own copy until it is deleted at cutover); a **neutral shim interface** decouples the register code from libuavcan types. **Shim:** `platforms/common/include/px4_platform_common/px4can.hpp` — a self-contained `px4can` namespace (CanFrame, ICanIface/ICanDriver, monotonic/UTC time, CanIOFlags, util helpers, Noncopyable) with **no libuavcan dependency**; `px4can::CanFrame` is layout-identical to both `uavcan::CanFrame` and the v0 `CanardCANFrame`. **Driver:** the three register sources (`uc_stm32h7_can/clock/thread`) + headers copied verbatim (battle-tested logic preserved) and neutralized with a one-line `namespace uavcan = px4can;` alias in `build_config.hpp` — the 1346-line register `.cpp` is otherwise unchanged. The TIM-based libuavcan clock is **dropped** for a tiny **hrt-backed clock** (no dedicated timer; canard gets PX4's monotonic timebase for free). **Wiring:** dronecan compiles the shared driver sources directly (safe: `dronecan ⊥ uavcan` are never co-linked, so no symbol clash) via a chip-guarded CMake branch (`CONFIG_ARCH_CHIP=stm32h7`, `-DDRONECAN_FDCAN_DRIVER=1`); `CanardFdcanIface` adapts `CanardCANFrame ↔ px4can::CanFrame` and registers the driver's CAN-RX-IRQ `BusEvent` → the node's `ScheduleNow()` trampoline (the **real** dual-scheduling source DESIGN §6 anticipated). **Proof:** the build links (FLASH **92.91 %**, the FDCAN driver adds ~4 KB — only the driver, not the libuavcan protocol core); the ELF carries `uavcan_stm32h7::CanIface::send(px4can::CanFrame const&, px4can::MonotonicTime, …)` (the driver speaks the neutral shim, not libuavcan) and `canardBroadcastObj`/`canardHandleRxFrame` (transport live via the node); the dronecan build has **zero `src/drivers/uavcan` references** (dep-file scan clean), so libuavcan stays deletable at cutover; codec test green; all new/changed files astyle-clean. **Build fixes:** `<limits>` is absent in the NuttX C++ context → shim uses `INT64_MAX`/`UINT64_MAX`; the driver instantiates `fill_n` with an enum count → shim's `fill_n` counts via an integer; `CanInitHelper::init` is overloaded `(uint32_t)`/`(uint32_t&)` → the adapter passes a prvalue to pick the fixed-bitrate overload. **Deferred:** generalizing to bxCAN (`uavcan_stm32`) + kinetis behind the same `px4can` seam; promoting the driver to a standalone `px4_add_library` once the `uavcan` module is also migrated to it; **on-bus hardware validation** (not doable in this environment). **Next:** P4.4 GNSS bridge.

**2026-05-30 — P4.4 GNSS bridge ✅ (links on `ark_fmu-v6x`; codec + bridge tests green).** The first/reference sensor bridge, in a new `src/drivers/dronecan/bridges/`. **Three layers, each one job:** (1) `SensorBridgeBase.{hpp,cpp}` — the `UavcanSensorBridgeBase` channel-alloc behavior (4 channels keyed by node_id, lazy `orb_advertise_multi` on first sighting, `_out_of_channels` latch, `get_channel_index_for_node`); deliberately does **not** inherit `device::Device` (the per-channel id is never the published one, ACCEPTANCE_SPEC §1), so device ids come from the pure `make_uavcan_device_id()` in `DeviceId.hpp` (platform-free, the documented LSB-first `bus_type:3|bus:5|address:8|devtype:8` pack; unit-tested against the spec's `node 125/CAN1/0x85 → 0x00857D03`). (2) `GnssDecode.{hpp,cpp}` — **platform-free** (generated DSDL structs + `sensor_gps_s` only, no uORB/hrt): every quirk a **named pure function** — `fix2_remap_fix_type` (mode/sub_mode→fix_type), `fix2_unpack_covariance` (the size-1/6/21/36 unpack), `unpack_square_matrix_3x3` (the Fix path), `leap_corrected_utc_usec` (the µs-scaled GPS+9/-leap, TAI-10/-leap, leap>0-only), `aux_is_fresh` (2 s), plus the templated `map_common` (the `process_fixx` body: lat/lon/alt, eph/epv, the velocity-covariance→heading Jacobian, vel/cog norm, UTC, dop selection, jamming/spoofing). (3) `Gnss.{hpp,cpp}` — an `RxSubscriberBase` that registers Fix(1060)/Fix2(1063)/Auxiliary(1061)/RelPosHeading(20006) `RxHandler`s with the node's `_rx_router` (+ MovingBaselineData(20005) when `DC_SUB_MBD`), `handle()` switches on `data_type_id`, decodes through `dronecan_codec::decodeTransfer`, runs the pure layer, and publishes `sensor_gps`; carries the global Auxiliary dop cache, the once-consumed RelPosHeading heading, the per-channel Fix-vs-Fix2 dedup, the Fix2 ECEF heading/noise/jamming **HACK**, the system-clock-set side effect, the RTCM/MovingBaselineData **uplink** from `gps_inject_data` (link failover, ≤8/cycle, 5 s rate, lowest priority via `encodeBroadcast`), and incoming MBD→`gps_dump` (hand-built device id, `len &= 0x7F`). **Wiring:** `DronecanNode` lazily `new`s the bridge in `init()` when `DC_SUB_GPS!=0`, registers its handlers, calls `_gnss->update()` **between `receive()` and the trailing `transmit()`** so uplink frames flush the same tick, surfaces it in `status`, and deletes it in the dtor; params `DC_SUB_GPS`/`DC_PUB_RTCM`/`DC_PUB_MBD`/`DC_SUB_MBD` added (+ the `UAVCAN_*→DC_*` migration rows). **Tests:** new `test/dronecan_bridge_test.cpp` + sibling `run_bridge_test.sh` — direct unit tests of each quirk **plus** a full `encode→broadcast→FakeCanardInterface loopback→receive→router→subscriber→decode→map_common` path asserting `sensor_gps` **field-for-field** (Fix2 + fresh Auxiliary dop; the Fix-vs-Fix2 dedup latch-lag; RelPosHeading heading consume-once) against the **real** generated `sensor_gps_s` (a tiny stubbed `test/stub/uORB/uORB.h` satisfies the topic header without linking uORB). `run_codec_test.sh` stays green. **Build:** `make ark_fmu-v6x` links; FLASH **93.37 %** (text 1,835,680 B) vs P4.3a 92.91 % — the slice now references the bridge + 6 GNSS codecs (Fix/Fix2/Auxiliary/RelPosHeading/RTCMStream/MovingBaselineData + ECEF/Timestamp) and the dual-interface media; the ~64 KB net reclaim is still the **P4.9** gate. `Gnss.cpp` 3993 B / `GnssDecode.cpp` **594 B** / `SensorBridgeBase.cpp` 394 B `.text`, **0 data/bss**; `map_common` + `make_uavcan_device_id` inline to nothing (no standalone symbols) — the flat-codec thesis holds at the bridge layer. 0 new warnings; all new files astyle-clean; **zero `src/drivers/uavcan` deps** (dep-scan clean). **Parity finding (flag for P7 review):** the Fix2 covariance switch is a **double** fallthrough — `case 21` → `case 36` → `default` (`gnss.cpp:277` *and* `:309`) — so sizes **21 and 36 both end `valid_covariances=false`** (eph/epv/s_variance/c_variance = -1), a stronger latent bug than the spec's "21→36 overwrite" note; replicated byte-identically and asserted in the test. **Multi-interface (CAN1 + CAN2), and a latent-bug fix found en route:** the bridge's `sensor_gps.device_id` bus field is the **real** CAN interface the transfer arrived on, plumbed end-to-end — `DronecanHandle::receive()` captures each frame's `iface_id`, `trampolineOnReception` forwards it, `DronecanRxRouter::dispatch(transfer, iface_id)` → `RxSubscriberBase::handle(transfer, iface_id)` → the bridge (`gps_dump` keeps its hand-built `bus=0` per spec). The `CanardFdcanIface` media is now genuinely multi-iface: it fans `receive()` round-robin across `getNumIfaces()` interfaces tagging each frame's source index, and `transmit()` broadcasts on the primary then best-effort on the secondaries (pop gated on the primary so no duplicates); the dronecan H7 build sets `-DUAVCAN_STM32H7_NUM_IFACES=2` (ark_fmu-v6x routes both CAN1+CAN2, `board_get_can_interfaces==0x3`, matching the legacy driver). **Latent bug fixed:** `CanardFdcanIface::receive()` never set `frame.iface_id` and `DronecanHandle::receive()` used an **uninitialized** `DronecanRxFrame`, so `iface_id` was garbage — and canard.c reads it for multi-frame reassembly (`same_iface`/iface-switch, canard.c:477/504, drops continuation frames on mismatch), which would have corrupted multi-frame RX (e.g. Fix2) on hardware; the frame is now zero-init + iface-tagged. The bridge test asserts iface 0→bus 0 and iface 1→bus 1 through the full path. **Deviations:** (1) `registerDeviceCapability`/`NodeInfoPublisher` is a no-op until **P4.6** (no `device_information` GPS rows until then) — matches the battery note pattern. (2) the diagnostic `gps_inject_data` lost-generation `PX4_WARN` was dropped (no behavior change). (3) **SITL N/A** — the dronecan module is `PLATFORM_NUTTX`-only; on-bus validation (now incl. **dual-interface** RX/TX + FDCAN2 bring-up) stays pending hardware (consistent with P4.3a). **Next:** P4.5 battery bridge.
